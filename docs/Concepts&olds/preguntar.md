Hey again! 👋

As I start planning the app more concretely, I have a couple of technical questions that would really help me move in the right direction:

1. **Do you have any internal tooling or documentation for reading/editing `.rgd` files?** Something like Relic's Attribute Editor or a custom tool the team uses? Having access to that (or even just knowing what's available) would let me pull unit stats directly from the mod files instead of relying on external wikis.

2. **Is there any documentation on the `.rec` replay file format?** Even partial notes on the binary structure (byte offsets for player names, factions, match result) would save a lot of reverse-engineering time.

The goal is to feed real mod data into the balance dashboard — so the win rate stats and power budget calculations are always in sync with whatever version of Unification is running.

No worries if this stuff is internal or undocumented — just wanted to ask before spending weeks figuring it out from scratch! 🙏